//717. 1-bit and 2-bit Characters

class Solution {
    public boolean isOneBitCharacter(int[] bits) {
        int one=0;
        for(int i=bits.length-2;i>=0 && bits[i]!=0;i--){
            one++;
        }
        return one%2!=0? false:true;
    }
}
